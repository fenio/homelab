---
# yaml-language-server: $schema=https://datreeio.github.io/CRDs-catalog/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: tns-csi
  namespace: disk
spec:
  interval: 5m
  timeout: 15m
  chart:
    spec:
      chart: tns-csi-driver
      version: "0.12.3"
      sourceRef:
        kind: HelmRepository
        name: tns-csi
        namespace: flux-system
  values:
    truenas:
      url: "wss://${CONFIG_TRUENAS_IP}:1443/api/current"
      apiKey: ${TRUENAS_API_KEY}
      skipTLSVerify: true

    image:
      repository: bfenski/tns-csi
      tag: "v0.12.3"
      pullPolicy: IfNotPresent

    controller:
      replicas: 1
      logLevel: 2

    storageClasses:
      - name: tns-csi-nfs
        enabled: true
        protocol: nfs
        pool: "storage"
        server: "10.10.20.100"
      - name: tns-csi-iscsi
        enabled: true
        protocol: iscsi
        pool: "storage"
        server: "10.10.20.100"
      - name: tns-csi-nvmeof
        enabled: true
        protocol: nvmeof
        isDefault: true
        pool: "storage"
        server: "10.10.20.100"

    rbac:
      create: true

    namespace: disk
